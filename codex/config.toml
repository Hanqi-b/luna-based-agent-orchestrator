# Luna-based Agent Orchestrator: Sol root + Luna subagents
#
# Main knobs to change:
#   model / model_reasoning_effort
#   agents.default_subagent_model
#   agents.default_subagent_reasoning_effort
#   agents.max_concurrent_threads_per_session
#
# This is intentionally project-scoped. Put it at:
#   <repo>/.codex/config.toml
#
# For a personal/global setup, merge the same settings into:
#   ~/.codex/config.toml

# Root / orchestrator. GPT-6 Sol high is the default; use max manually for
# exceptional tasks that need additional root reasoning.
model = "gpt-6-sol"
model_reasoning_effort = "high"

# Safe interactive defaults. Change if your workflow needs otherwise.
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[agents]
enabled = true

# Number of child agent threads Codex may keep open concurrently.
# Maximum of 4; spawn only for genuinely independent work.
max_concurrent_threads_per_session = 4

# Default for any spawned subagent that does not explicitly choose a model.
# Luna execution roles use maximum reasoning; the root and reviewer settings are
# pinned in their respective configuration files.
default_subagent_model = "gpt-6-luna"
default_subagent_reasoning_effort = "max"
